<%= form_with(model: agreement, local: true) do |form| %>
  <%= form_errors agreement %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <%= form.submit class: "btn btn-primary" %>
<% end %>
